# JWT Secret (change this!)
JWT_SECRET=your-super-secret-key-change-this-in-production

# Database paths
AUTH_DB_PATH=data/auth.db      # Managed by CrydenSync
NOTES_DB_PATH=data/notes.db     # Managed by Typebook

# Server port
PORT=3000
